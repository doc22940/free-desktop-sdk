# Project-wide configuration of meson elements
elements:
  meson:
    variables:
      meson-global: >-
        --buildtype=plain
        -Dauto_features=enabled
      ninja: |
        ninja -v -j ${NINJAJOBS} -C %{build-dir}
